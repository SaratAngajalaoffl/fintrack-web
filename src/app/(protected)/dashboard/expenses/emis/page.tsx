import { DashboardPlaceholderPage } from "@/components/ui/layout/dashboard";

export const metadata = {
  title: "EMIs — Fintrack",
};

export default function ExpenseEmisPage() {
  return <DashboardPlaceholderPage title="EMIs" />;
}
